import { styled } from '../../../stitches.config';

export const PageTitleContainer = styled('div', {
  display: 'flex',
  alignItems: 'center',
  gap: '$3',

  svg: {
    color: '$green100',
  },
});
